#!/bin/bash

set -e

if [ "$1" = "--help" ]; then
	echo "fman version ${version}"
	echo "Usage: fman [left_dir [right_dir]]    open the given directories"
	exit 0
fi

(/opt/fman/fman "$@" > /dev/null 2>&1 &)